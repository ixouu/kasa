import React from 'react';

const Dropdown = ({ props, title }) => {
    console.log(props);
    // console.log(title);
    return (
        <div>
            Dropdown
        </div>
    );
}

export default Dropdown;
